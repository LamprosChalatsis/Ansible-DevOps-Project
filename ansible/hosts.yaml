---
  # VM #1: appserver + Jenkins (Docker environment, CI/CD)
  # ΣΗΜΕΙΩΣΗ: δεν ορίζουμε πια ansible_ssh_private_key_file εδώ.
  # Η ταυτότητα (identity) δίνεται από έναν SSH agent -- είτε το δικό σου
  # (όταν τρέχεις χειροκίνητα από WSL), είτε αυτόν που φτιάχνει το
  # Jenkins credentials plugin (sshagent step) όταν τρέχει το Jenkins.
  appservers:
    hosts:
      appserver:
        ansible_host: 34.140.150.84
        ansible_user: lambr
 
  # Ίδιο VM με το appservers -- χρησιμοποιείται μόνο όταν τρέχεις το
  # playbooks/postgres.yaml (bare-metal Postgres, plain-VM environment).
  dbservers:
    hosts:
      appserver:
 
  # VM #2: αποκλειστικά για Kubernetes/Microk8s
  k8sservers:
    hosts:
      k8snode:
        ansible_host: 35.195.166.77
        ansible_user: lambr
